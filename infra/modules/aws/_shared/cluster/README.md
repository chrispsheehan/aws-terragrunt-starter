# `_shared/cluster`

Shared ECS cluster module.

## Owns

- ECS cluster

## Key outputs

- `cluster_id`
- `cluster_name`

Used by ECS service modules and workflows that need the target cluster identity.
